image: node:latest

cache:
  untracked: true
  key: "$CI_BUILD_REF_NAME"
  paths:
    - node_modules/
    - dist/

stages:
  - setup
  - test
  - build
  - deploy


# install node packages
setup:
  stage: setup
  script:
    - npm install

# run karma
tests:
  stage: test
  script:
    - npm run test

# build dist
build:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/

